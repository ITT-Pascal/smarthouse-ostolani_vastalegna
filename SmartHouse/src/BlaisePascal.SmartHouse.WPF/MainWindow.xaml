<Window x:Class="BlaisePascal.SmartHouse.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BlaisePascal.SmartHouse.WPF"
        mc:Ignorable="d"
        Title="SMARTHOUSE" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="Lamps" FontSize="18" Grid.Row="0" Margin="0,0,0,10"/>

        <!-- Lamp list -->
        <ListBox Name="lampList"
             Grid.Row="1"
             SelectionMode="Single"
             SelectionChanged="LampList_SelectionChanged"/>

        <!-- Text box -->
        <TextBox Name="messageBox"
             Grid.Row="2"
             IsReadOnly="True"
             Text="Seleziona un’operazione"
             Background="Azure"
             Margin="0,5,0,5"/>

        <!-- Input -->
        <StackPanel Orientation="Horizontal" Grid.Row="3" Margin="0,5,0,5">
            <TextBlock Text="Input:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Name="inputBox" Width="150"/>
            <Button Content="Invia" Margin="5,0,0,0" Click="SubmitInput_Click"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="Add" Click="AddLamp_Click" Margin="5"/>
            <Button Content="Remove" Click="RemoveLamp_Click" Margin="5"/>
            <Button Content="On" Click="SwitchOnLamp_Click" Margin="5"/>
            <Button Content="Off" Click="SwitchOffLamp_Click" Margin="5"/>
            <Button Content="Brighten" Click="BrightenLamp_Click" Margin="5"/>
            <Button Content="Dimmer" Click="DimmerLamp_Click" Margin="5"/>
            <Button Content="Set brightness" Click="SetBrightnessLamp_Click" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
